server:
  port: 8081

logging:
  level:
    root: info

spring:
  mvc:
    path match:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://localhost:3306/bes2-db
    username : root
    password: 1234

